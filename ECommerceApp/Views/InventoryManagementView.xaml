<UserControl x:Class="ECommerceApp.Views.InventoryManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="700"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WarningButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#FF9800"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F57C00"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Inventory Management"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333333"/>
            <TextBlock Text="Monitor and update stock levels"
                       FontSize="12"
                       Foreground="#666666"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Low Stock Alert -->
            <Border Grid.Column="0" Background="#FFEBEE" CornerRadius="5" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Low Stock Items" FontSize="12" Foreground="#C62828"/>
                    <TextBlock Text="{Binding LowStockCount}" FontSize="28" FontWeight="Bold" Foreground="#F44336"/>
                </StackPanel>
            </Border>

            <!-- Total Products (placeholder) -->
            <Border Grid.Column="1" Background="#E3F2FD" CornerRadius="5" Padding="15" Margin="5,0">
                <StackPanel>
                    <TextBlock Text="Total Products" FontSize="12" Foreground="#1565C0"/>
                    <TextBlock Text="{Binding InventoryItems.Count}" FontSize="28" FontWeight="Bold" Foreground="#2196F3"/>
                </StackPanel>
            </Border>

            <!-- Actions -->
            <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="15" Margin="10,0,0,0">
                <StackPanel>
                    <Button Content="Show Low Stock Only"
                            Style="{StaticResource WarningButtonStyle}"
                            Command="{Binding ToggleLowStockFilterCommand}"
                            HorizontalAlignment="Stretch"/>
                    <Button Content="Refresh"
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#757575"
                            Command="{Binding RefreshCommand}"
                            HorizontalAlignment="Stretch"
                            Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Inventory Grid -->
            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="15" Margin="0,0,15,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <DataGrid ItemsSource="{Binding InventoryItems}"
                          SelectedItem="{Binding SelectedInventory}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="Transparent"
                          RowBackground="White"
                          AlternatingRowBackground="#FAFAFA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Product" Binding="{Binding Product.ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Product.Category.CategoryName}" Width="120"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="80"/>
                        <DataGridTextColumn Header="Min Stock" Binding="{Binding MinimumStock}" Width="80"/>
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="OK"/>
                                                <Setter Property="Foreground" Value="#4CAF50"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                        <Setter Property="Text" Value="Low Stock"/>
                                                        <Setter Property="Foreground" Value="#F44336"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Stock Update Panel -->
            <Border Grid.Column="1" Background="White" CornerRadius="5" Padding="20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="Update Stock"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Selected Product:"
                               FontSize="12"
                               Foreground="#666666"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding SelectedInventory.Product.ProductName, FallbackValue='Select a product'}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Current Stock:"
                               FontSize="12"
                               Foreground="#666666"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding SelectedInventory.StockQuantity, FallbackValue='0'}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#333333"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Quantity:"
                               FontSize="12"
                               Foreground="#666666"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NewStockQuantity, UpdateSourceTrigger=PropertyChanged}"
                             Padding="10,8"
                             Margin="0,0,0,20"/>

                    <Button Content="Set Stock"
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding UpdateStockCommand}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8"/>

                    <Button Content="Add Stock (+)"
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#2196F3"
                            Command="{Binding AddStockCommand}"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,8"/>

                    <Button Content="Remove Stock (-)"
                            Style="{StaticResource DangerButtonStyle}"
                            Command="{Binding RemoveStockCommand}"
                            HorizontalAlignment="Stretch"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Messages -->
        <StackPanel Grid.Row="3" Margin="0,15,0,0">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#F44336"
                       FontSize="12"/>
            <TextBlock Text="{Binding SuccessMessage}"
                       Foreground="#4CAF50"
                       FontSize="12"/>
        </StackPanel>
    </Grid>
</UserControl>
