<UserControl x:Class="ECommerceApp.Views.ProductManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="700"
             Background="Transparent">

    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#2196F3"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Product Management"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333333"/>
            <TextBlock Text="Manage your store products"
                       FontSize="12"
                       Foreground="#666666"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <StackPanel Orientation="Horizontal" Grid.Column="0">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Width="250"
                             Padding="10,8"
                             FontSize="12"
                             VerticalContentAlignment="Center"/>
                    <Button Content="Search"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding SearchCommand}"
                            Margin="10,0,0,0"/>
                    <Button Content="Refresh"
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding RefreshCommand}"
                            Background="#757575"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button Content="+ Add Product"
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding AddProductCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <!-- Products Grid (when not editing) -->
            <Border Background="White" CornerRadius="5" Padding="15"
                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}, ConverterParameter=Inverse}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <DataGrid ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="Transparent"
                          RowBackground="White"
                          AlternatingRowBackground="#FAFAFA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="50"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category.CategoryName}" Width="120"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C2}" Width="100"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding Inventory.StockQuantity}" Width="80"/>
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Edit Form (when editing) -->
            <Border Background="White" CornerRadius="5" Padding="20"
                    Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}}">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <StackPanel MaxWidth="500">
                    <TextBlock Text="{Binding IsNewProduct, Converter={StaticResource BoolToVisibility}, ConverterParameter=Add New Product}"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Product Name" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditProductName, UpdateSourceTrigger=PropertyChanged}"
                             Padding="10,8" Margin="0,0,0,15"/>

                    <TextBlock Text="Description" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding EditDescription, UpdateSourceTrigger=PropertyChanged}"
                             Padding="10,8" Height="80" TextWrapping="Wrap"
                             AcceptsReturn="True" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Price" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding EditPrice, UpdateSourceTrigger=PropertyChanged}"
                                     Padding="10,8"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Category" FontSize="12" Foreground="#666666" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Categories}"
                                      SelectedItem="{Binding EditCategory}"
                                      DisplayMemberPath="CategoryName"
                                      Padding="10,8"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Image URL" FontSize="12" Foreground="#666666" Margin="0,15,0,5"/>
                    <TextBox Text="{Binding EditImageURL, UpdateSourceTrigger=PropertyChanged}"
                             Padding="10,8" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel"
                                Style="{StaticResource ActionButtonStyle}"
                                Background="#757575"
                                Command="{Binding CancelEditCommand}"/>
                        <Button Content="Save Product"
                                Style="{StaticResource ActionButtonStyle}"
                                Command="{Binding SaveProductCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Bottom Actions and Messages -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Messages -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#F44336"
                           FontSize="12"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisibility}}"/>
                <TextBlock Text="{Binding SuccessMessage}"
                           Foreground="#4CAF50"
                           FontSize="12"
                           Visibility="{Binding SuccessMessage, Converter={StaticResource BoolToVisibility}}"/>
            </StackPanel>

            <!-- Edit/Delete Buttons -->
            <StackPanel Orientation="Horizontal" Grid.Column="1"
                        Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibility}, ConverterParameter=Inverse}">
                <Button Content="Edit"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding EditProductCommand}"/>
                <Button Content="Delete"
                        Style="{StaticResource DangerButtonStyle}"
                        Command="{Binding DeleteProductCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
